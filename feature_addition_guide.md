#for adding features
first add required ui design changes along with required id,class to index.html
then add required actions in javascript by adding event listener for that button/feature
for connecting supabase make connection.py first and connect,send supabase id and password to me for adding in .env file

add required routes in main.py at end for sending and receiving
comment after adding.